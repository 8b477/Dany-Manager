#### Start npm:

```shell
npm install
```

#### Start composer :

```shell
composer install
```

#### Start server PHP (localhost:8000)

```shell
npm run php
```

#### Watch css (Tailwind)

```shell
npm run css
```
